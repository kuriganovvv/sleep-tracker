# 💤 Sleep Tracker — Frontend Course Project

## 📌 Описание проекта

Sleep Tracker — это одностраничное Angular-приложение для учета, анализа и визуализации сна. Приложение позволяет фиксировать ночной и дневной сон, учитывать факторы влияния (кофеин, активность), анализировать циркадные ритмы и отображать данные в интуитивном интерфейсе.

Технологический стек: Angular 17+, RxJS, Angular Material, TypeScript, SCSS
---

## 🎓 Курсовая работа

### Требования к проекту

- Использование **Standalone-компонентов**(Все компоненты созданы как standalone)
- Использование **Reactive Forms**(	Формы ввода сна с валидацией)
- Применение **RxJS** для реактивного управления данными(BehaviorSubject для состояния, HTTP-запросы к API погоды)
- Использование **Dependency Injection (DI)**(Сервисы для работы с данными, Material компоненты)
- Использование **Angular Material**(	Таблицы, кнопки, меню, формы, иконки)
- Тестирование работы некоторых сервисов и компонентов на различных кейсах(	Unit-тесты для сервисов)
- Реализация визуально аккуратного и понятного интерфейса

---
## Структура проекта
```bash
src/app/
├── components/
│   ├── sleep-form/      # Форма ввода данных о сне
│   ├── sleep-log/       # Таблица истории сна
│   ├── sleep-mode/      # Режим сна с часами
│   └── navigation/      # Навигационная панель
├── services/
│   ├── sleep.service.ts # Логика работы с данными сна
│   └── storage.service.ts # Абстракция над localStorage
├── models/
│   ├── sleep-day.model.ts     # Модель дня сна
│   └── sleep-interval.model.ts # Модель интервала сна
├── pipes/
│   └── sleep-bar.pipe.ts      # Визуализация сна в текстовом виде
└── app.config.ts             # Конфигурация приложения
```
## 🧩 Основной функционал

### 🔹 Режим сна
- Полноэкранный режим с звездами
- Текущее время крупным шрифтом.
- Время восхода/заката солнца (через OpenWeatherMap API)
- Подсказка для релаксации перед сном

Технологии: Fullscreen API, RxJS interval, HTTPClient
---

### 🔹 Ввод данных о сне
- Form с поддержкой нескольких интервалов сна за день
- Автоматический расчет длительности сна
- Валидация времени (сон через полночь, корректные интервалы)
- Оценка качества сна (1-5 звезд)
- Учет дополнительных факторов (примечания)
**Технологии: Reactive Forms, ngModel, Material Datepicker**

---

### 🔹 Лог сна
- Таблица с фильтрацией по месяцам
- Визуализация сна через sleep-bar pipe (текстовый график)
- Inline-редактирование записей прямо в таблице
- Статистика по месяцу (среднее время сна, оценка)
- Удаление и редактирование через контекстное меню
**Технологии: MatTable, Pipes, @Output события**

---

### 🔹 Анализ и расчеты
- Автоматическое разделение интервалов, пересекающих полночь
- Корректный расчет общей длительности сна
- Статистика по месяцам/неделям

---

### 🔹 Интеграция с внешним API
- Получение времени восхода/заката для текущей локации
- Обработка ошибок сети, fallback-значения
- Технологии: HttpClient, async/await, error handling

---

### 🔹 Локальное хранение данных
- Все данные хранятся в `localStorage`
- Логика работы с хранилищем инкапсулирована в отдельном сервисе
- Используется `BehaviorSubject` для реактивного обновления UI

---

## 🧪 Тестирование

Реализованы юнит-тесты для:
- сервиса управления сном
- сервиса работы с `localStorage`
- отдельных компонентов интерфейса

Тестируются:
- алгоритмы расчетов
- обработка крайних случаев
- корректность валидации форм

---

## 🎨 UI и UX
- Используется **Angular Material**
- Минималистичный интерфейс
- Ориентация на использование в вечернее и ночное время

---

## 📝 Автор

**Куриганов Влад**  
Группа: **ИВ-422**  
Курсовая работа по frontend-разработке  
2025
